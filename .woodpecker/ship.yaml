# Central ship pipeline — triggered by `pc ship` (manual) with pipeline variables.
# Secrets on personal-cloud repo: ghcr_token, github_clone_token (optional)

when:
  - event: manual

# Postgres for optional app test steps (hostname: postgres).
services:
  - name: postgres
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: ci
      POSTGRES_DB: app

steps:
  - name: validate-vars
    image: alpine:3.20
    commands:
      - |
        set -eu
        for v in APP_NAME SOURCE_REPO SOURCE_REF IMAGE IMAGE_REPO IMAGE_TAG DO_BUILD EXPOSURE ROUTE_HOST SERVICE_CONTAINER SERVICE_PORT COMPOSE_TEMPLATE COMPOSE_ENV_FILE BUILD_CONTEXT BUILD_DOCKERFILE GITHUB_OWNER; do
          eval "val=\$$v"
          if [ -z "$val" ]; then
            echo "missing required variable: $v" >&2
            exit 1
          fi
        done
        echo "ship ${APP_NAME} ${IMAGE}:${IMAGE_TAG} exposure=${EXPOSURE} host=${ROUTE_HOST}"

  - name: clone-app
    image: alpine/git:2.45.2
    environment:
      GITHUB_CLONE_TOKEN:
        from_secret: github_clone_token
    commands:
      - |
        set -eu
        rm -rf app-src
        if [ -n "${GITHUB_CLONE_TOKEN:-}" ]; then
          git clone "https://x-access-token:${GITHUB_CLONE_TOKEN}@github.com/${SOURCE_REPO}.git" app-src
        else
          git clone "https://github.com/${SOURCE_REPO}.git" app-src
        fi
        cd app-src
        git checkout "${SOURCE_REF}"

  - name: test
    image: rust:1.88-bookworm
    environment:
      DATABASE_URL: postgres://app:ci@postgres:5432/app
      JWT_SECRET: ci-test-jwt-secret
    commands:
      - |
        set -eu
        if [ -z "${TEST_SCRIPT:-}" ]; then
          echo "no TEST_SCRIPT, skipping"
          exit 0
        fi
        apt-get update -qq
        apt-get install -y -qq postgresql-client
        echo "waiting for postgres..."
        for i in $(seq 1 30); do
          if pg_isready -h postgres -U app -q; then
            break
          fi
          sleep 1
        done
        pg_isready -h postgres -U app -q
        cd app-src
        eval "${TEST_SCRIPT}"
    when:
      - evaluate: 'len(TEST_SCRIPT) > 0'

  - name: build-and-push
    image: docker:26-cli
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      GHCR_TOKEN:
        from_secret: ghcr_token
    commands:
      - echo "$$GHCR_TOKEN" | docker login ghcr.io -u "$$GITHUB_OWNER" --password-stdin
      - |
        docker buildx build --push \
          -t "${IMAGE}:${IMAGE_TAG}" -t "${IMAGE}:latest" \
          -f "app-src/${BUILD_DOCKERFILE}" "app-src/${BUILD_CONTEXT}"
    when:
      - evaluate: 'DO_BUILD == "true"'

  - name: provision
    image: alpine:3.20
    volumes:
      - /opt/personal-cloud:/opt/personal-cloud
    commands:
      - apk add --no-cache gettext bash
      - chmod +x /opt/personal-cloud/scripts/render-app.sh
      - PC_ROOT=/opt/personal-cloud /opt/personal-cloud/scripts/render-app.sh

  - name: deploy
    image: docker:26-cli
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /opt/personal-cloud:/opt/personal-cloud
    commands:
      - |
        set -eu
        export IMAGE_TAG="${IMAGE_TAG}"
        COMPOSE_FILE="/opt/personal-cloud/apps/${APP_NAME}/compose.yaml"
        echo "$$GHCR_TOKEN" | docker login ghcr.io -u "$$GITHUB_OWNER" --password-stdin
        docker compose -f "$COMPOSE_FILE" --env-file "$COMPOSE_ENV_FILE" pull
        docker compose -f "$COMPOSE_FILE" --env-file "$COMPOSE_ENV_FILE" up -d
    environment:
      GHCR_TOKEN:
        from_secret: ghcr_token

  - name: reload-caddy
    image: docker:26-cli
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    commands:
      - docker exec caddy caddy reload --config /etc/caddy/Caddyfile

  - name: health
    image: docker:26-cli
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    commands:
      - |
        set -eu
        sleep 5
        docker run --rm --network personal-cloud_edge curlimages/curl:8.7.1 \
          -fsS "http://${SERVICE_CONTAINER}:${SERVICE_PORT}${SERVICE_HEALTH_PATH:-/health}" \
          || echo "health check failed (app may still be starting)"
